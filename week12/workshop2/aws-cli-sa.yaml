apiVersion: v1
kind: Pod
metadata:
  name: aws-cli
  namespace: lab4
spec:
  serviceAccountName: simranch
  containers:
  - name: aws-cli
    image: amazon/aws-cli:latest
    command:
      - sleep
      - "3600"
    imagePullPolicy: IfNotPresent
  restartPolicy: Always